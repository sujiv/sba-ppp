import { LoanFormDataModel } from './loan-form-data-model';

describe('LoanFormDataModel', () => {
  it('should create an instance', () => {
    expect(new LoanFormDataModel()).toBeTruthy();
  });
});
