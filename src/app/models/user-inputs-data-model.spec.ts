import { UserInputsDataModel } from './user-inputs-data-model';

describe('UserInputsDataModel', () => {
  it('should create an instance', () => {
    expect(new UserInputsDataModel()).toBeTruthy();
  });
});
